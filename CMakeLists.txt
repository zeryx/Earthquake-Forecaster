project(Earthquake_Forecaster)
find_package(CUDA REQUIRED)
cmake_minimum_required(VERSION 2.8)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/tinyxml2)
set(CUDA_NVCC_FLAGS ${CUDA_NVCC_FLAGS} -gencode arch=compute_30,code=sm_30)
CUDA_ADD_EXECUTABLE(${PROJECT_NAME} tinyxml2/tinyxml2.cpp network.h dataarray.h getsys.h  network.cu main.cpp)
#TARGET_LINK_LIBRARIES(${PROJECT_NAME} dlib)
